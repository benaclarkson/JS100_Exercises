/**
 * Predict the output of the following code:
 * 
 * Solution
 * The following code will output the string `'No...'` to the console because
 * the `if` conditional being checked on `line 9` evaluates to false.
 */

if (true && false) {
  console.log('Yes!');
} else {
  console.log('No...');
}