/**
 * Predict the output of the following code:
 * 
 * Solution
 * The following code will output the string `'Yes!'` to the console because
 * the `if` conditional being checked on `line 9` evaluates to true.
 */

if (false || true) {
  console.log('Yes!');
} else {
  console.log('No...');
}