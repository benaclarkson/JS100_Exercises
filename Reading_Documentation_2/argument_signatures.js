/* How many arguments does the Array.prototype.join() method expect? What
// happens if you call it with less or more arguments?

// Solution
// The `Array.prototype.join()` method doesn't expect any arguments.
// If no arguments are provided, the method defaults to using a comma to join
// the elements of the array. If more than one argument is provided, only the
// first argument is used to join each element of the array. The other arguments
// are ignored.
*/

